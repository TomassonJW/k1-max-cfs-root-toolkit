# Exclusion réelle du propriétaire stock CFS V1

Cette gate à usage unique a désactivé l’auto-remplacement stock une fois, prouvé
deux fois la valeur `0`, puis restauré une fois la valeur sauvegardée `1` et
prouvé deux fois le retour exact.

La même connexion Moonraker est restée ouverte. Aucune transition CFS, dérive
de mesh, Z, chauffe, axe, route ou configuration n’a été observée. Aucun effet
filament, mouvement, fichier distant ou service n’a eu lieu.

Le pilote et la gate sont consommés : `rerun_authorized=false`.
